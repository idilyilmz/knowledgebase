# JavaScript / ecosystem
Stuff around JavaScript, not JS itself, tools & workflows

## 1. Package managers

### npm basics
Use **npm** 
- if you want the default (ships with Node.js)
- for smaller projects or when following tutorials/docs
- if you want built-in security auditing

### yarn
Use **Yarn**
- if you're working with **monorepos** (using workspaces)
- for faster and more reliable installs in large projects
- if you need strong offline support

<strong>pnpm</strong>

## 2. Bundler
<strong>Vite</strong>

<strong>Webpack </strong>(entry, loaders, plugins)

<strong>esbuild</strong>

<strong>Parcel</strong>

## 3. Transpilers
<strong>Babel </strong>(why it exists, presets)

## 4. Linters / Formatters
<strong>ESLint basics</strong>

<strong>Prettier rules</strong>

<strong>Common configs</strong>

## 5. Package.json
<strong>Scripts</strong>

<strong>Dependencies vs devDependencies</strong>

<strong>Version ranges</strong> (^ ~ etc.)

<strong>npx</strong>

## 6. Build / Dev workflow
<strong>Dev servers</strong>

<strong>Hot module replacement</strong>

<strong>Tree shaking</strong>

<strong>Code splitting</strong>