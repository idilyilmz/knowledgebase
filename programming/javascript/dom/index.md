# JavaScript (DOM)
Everything related to interacting with the webpage and UI.

## 1. Foundational DOM topics
<strong>What the DOM is</strong> (tree structure)

<strong>Selecting elements</strong>(`querySelector`, `getElementById`)

<strong>Selecting elements</strong>(`querySelector`, `getElementById)

<strong>Creating & inserting elements</strong>(`createElement`, `append`, `prepend`)

<strong>Removing & replacing elements</strong>

<strong>Modifying attributes & properties</strong>

<strong>Modifying CSS</strong>(inline, classes)

<strong>Understanding DOM reflow & repaint</strong>

## 2. Events
<strong>Adding & removing event listeners</strong>

<strong>Event bubbling vs. capturing</strong>

<strong>Event delegation</strong>(important!)

<strong>Common events: click, input, submit, scroll, keydown</strong>

## 3. DOM traversal
<strong>parentNode / children / siblings</strong>

<strong>closest()</strong>

## 4. Rendering & perfomance
<strong>Minimizing reflows</strong>

<strong>Using DeocumentFragment</strong>

<strong>Virtual DOM vs real DOM</strong>(conceptual comparison)